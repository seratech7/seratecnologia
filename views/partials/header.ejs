<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | <%= typeof siteConfig !== 'undefined' && siteConfig.site_name ? siteConfig.site_name : 'MarketPlace' %></title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/market-hacker.css">
  <link rel="stylesheet" href="/fonts/fontawesome.min.css">
  <link rel="stylesheet" href="/css/notifications.css">
  <link rel="manifest" href="/manifest.webmanifest">
  <meta name="theme-color" content="#1565c0">
  <% if (typeof og !== 'undefined' && og) { %>
  <meta property="og:type" content="article">
  <meta property="og:title" content="<%= og.title %>">
  <meta property="og:description" content="<%= og.description %>">
  <meta property="og:image" content="<%= og.image %>">
  <meta property="og:url" content="<%= og.url %>">
  <meta name="twitter:card" content="summary_large_image">
  <% } %>
  <script>
    if (localStorage.getItem('hacker-theme') === '1') {
      document.documentElement.classList.add('hacker-theme');
    }
  </script>
  <% if (typeof siteConfig !== 'undefined' && siteConfig.site_favicon_url) { %><link rel="icon" href="<%= siteConfig.site_favicon_url %>"><% } else { %><link rel="icon" href="/favicon.ico"><% } %>
  <% if (typeof customCSS !== 'undefined' && customCSS) { %><style><%= customCSS %></style><% } %>
  <% if (typeof customJS !== 'undefined' && customJS) { %><script><%= customJS %></script><% } %>
</head>
<body>
<script>
  if (localStorage.getItem('hacker-theme') === '1') {
    document.body.classList.add('hacker-theme');
  }
  </script>

<div class="wa-topbar">
  <a href="<%= typeof whatsappGroupLink !== 'undefined' ? whatsappGroupLink : 'https://chat.whatsapp.com/SEU-GRUPO-AQUI' %>" target="_blank" rel="noopener" class="wa-topbar-link">
    <i class="fab fa-whatsapp"></i>
    <span>Entre no nosso Grupo VIP no WhatsApp — Clique aqui para entrar no nosso grupo</span>
  </a>
</div>
<style>
:root { --wa-bar: 36px; }
.wa-topbar { position: fixed; top: 0; left: 0; right: 0; z-index: 1001; background:#ffd400; color:#1a1400; height: var(--wa-bar); display:flex; align-items:center; justify-content:center; overflow:hidden; }
.wa-topbar-link { display:flex; align-items:center; justify-content:center; gap:8px; height:100%; width:100%; padding:0 12px; font-size:13px; font-weight:700; text-decoration:none; color:#1a1400; text-align:center; white-space:nowrap; }
.wa-topbar-link i { font-size:17px; flex:none; }
.wa-topbar-link:hover { background:rgba(0,0,0,.07); }
.main-header { top: var(--wa-bar); }
.top-ad { top: calc(var(--header-height) + var(--wa-bar)); }
.main-content { margin-top: calc(var(--header-height) + var(--wa-bar)); min-height: calc(100vh - var(--header-height) - var(--wa-bar)); }
@media (max-width:600px){
  :root { --wa-bar: 52px; }
  .wa-topbar-link { font-size:11px; white-space:normal; }
}
body.hacker-theme .wa-topbar { background:#ffd400; color:#1a1400; }
body.hacker-theme .wa-topbar-link,
body.hacker-theme .wa-topbar-link:hover { color:#1a1400; }
body.hacker-theme .wa-topbar-link:hover { background:rgba(0,0,0,.07); }
</style>

<header class="main-header">
  <div class="header-container">
    <div class="header-left">
      <button class="hamburger" id="hamburgerBtn" aria-label="Menu">
        <span></span><span></span><span></span>
      </button>
      <a href="/" class="logo">
        <% if (typeof siteConfig !== 'undefined' && siteConfig.site_logo_url) { %>
          <img src="<%= siteConfig.site_logo_url %>" alt="<%= siteConfig.site_name || 'Logo' %>" style="max-height:40px;max-width:180px;object-fit:contain;">
        <% } else { %>
          <i class="fas fa-microchip"></i>
          <span><%= typeof siteConfig !== 'undefined' && siteConfig.site_name ? siteConfig.site_name : 'SeraTecnologia' %></span>
        <% } %>
      </a>
    </div>
    <div class="header-center" id="searchContainer">
      <form action="/marketplace" method="GET" class="search-form">
        <i class="fas fa-search"></i>
        <input type="text" name="search" placeholder="Buscar HD, SSD, notebook..." value="<%= typeof search !== 'undefined' ? search : '' %>">
        <button type="submit">Buscar</button>
      </form>
    </div>
    <div class="header-right">
      <button class="theme-toggle-btn" id="themeToggle" onclick="toggleHackerTheme()" title="Alternar tema claro/escuro" aria-label="Alternar tema claro/escuro">
        <i class="fas fa-terminal"></i>
      </button>
      <a href="/noticias" class="btn-admin-header" style="margin-right:4px;">
        <i class="fas fa-newspaper"></i>
        <span>Notícias</span>
      </a>
      <a href="/marketplace" class="btn-admin-header" style="margin-right:4px;">
        <i class="fas fa-shopping-cart"></i>
        <span>Marketplace</span>
      </a>
      <a href="/logins" class="btn-admin-header" style="margin-right:4px;">
        <i class="fas fa-key"></i>
        <span>Logins</span>
      </a>
      <a href="/rastreio" class="btn-admin-header" style="margin-right:4px;">
        <i class="fas fa-truck"></i>
        <span>Rastrear</span>
      </a>
      <div class="notif-center">
        <button class="notif-bell-btn" data-endpoint="/api/notifications/recent" data-all-link="/notificacoes" aria-label="Notificações">
          <i class="fas fa-bell"></i>
          <span class="notif-badge" hidden>0</span>
        </button>
        <div class="notif-panel">
          <div class="notif-panel-head">
            <strong><i class="fas fa-bell"></i> Notificações <span class="notif-count-pill">0</span></strong>
            <button class="notif-mark-all">Marcar todas como lidas</button>
          </div>
          <div class="notif-list"></div>
          <div class="notif-panel-foot"><a href="/notificacoes">Ver todas</a></div>
        </div>
      </div>
      <button class="search-toggle-mobile" id="searchToggle" aria-label="Buscar">
        <i class="fas fa-search"></i>
      </button>
      <% if (admin) { %>
        <a href="/admin/dashboard" class="btn-admin-header">
          <i class="fas fa-cog"></i>
          <span>Admin</span>
        </a>
      <% } else if (seller) { %>
        <a href="/seller/dashboard" class="btn-admin-header">
          <i class="fas fa-store-alt"></i>
          <span>Vendedor</span>
        </a>
      <% } else if (customer) { %>
        <a href="/conta" class="btn-admin-header">
          <i class="fas fa-user"></i>
          <span>Minha Conta</span>
        </a>
      <% } else { %>
        <a href="/login" class="btn-admin-header">
          <i class="fas fa-user"></i>
          <span>Entrar</span>
        </a>
        <a href="/seller/login" class="btn-admin-header">
          <i class="fas fa-store-alt"></i>
          <span>Vender</span>
        </a>
      <% } %>
    </div>
  </div>
</header>

<div class="mobile-overlay" id="mobileOverlay"></div>
<nav class="mobile-menu" id="mobileMenu">
  <div class="mobile-menu-header">
    <a href="/" class="mobile-logo"><i class="fas fa-microchip"></i> SeraTecnologia</a>
    <button class="mobile-close" id="mobileClose">&times;</button>
  </div>
  <div class="mobile-menu-body">
    <form action="/marketplace" method="GET" class="mobile-search-form">
      <input type="text" name="search" placeholder="Buscar produtos..." value="<%= typeof search !== 'undefined' ? search : '' %>">
      <button type="submit"><i class="fas fa-search"></i></button>
    </form>
    <div class="mobile-nav-links">
      <a href="/noticias"><i class="fas fa-newspaper"></i> Notícias & Hacking</a>
      <a href="/marketplace"><i class="fas fa-shopping-cart"></i> Marketplace</a>
      <a href="/logins"><i class="fas fa-key"></i> Logins & Contas</a>
      <a href="/rastreio"><i class="fas fa-truck"></i> Rastrear Pedido</a>
      <a href="#" onclick="toggleHackerTheme();return false;"><i class="fas fa-terminal"></i> Tema Hacker</a>
      <a href="#" class="mobile-sub-toggle" id="catToggle"><i class="fas fa-th-large"></i> Categorias <i class="fas fa-chevron-down"></i></a>
      <div class="mobile-submenu" id="catSubmenu">
        <a href="/marketplace">Todas</a>
        <a href="/marketplace?category=ssds">⚡ SSDs</a>
        <a href="/marketplace?category=hds-armazenamento">💾 HDs</a>
        <a href="/marketplace?category=memoria-ram">🧠 Memória RAM</a>
        <a href="/marketplace?category=processadores">🔲 Processadores</a>
        <a href="/marketplace?category=placas-video">🎮 Placas de Vídeo</a>
        <a href="/marketplace?category=placas-mae">🔧 Placas-mãe</a>
      </div>
      <% if (admin) { %>
        <a href="/admin/dashboard"><i class="fas fa-cog"></i> Painel Admin</a>
      <% } else if (seller) { %>
        <a href="/seller/dashboard"><i class="fas fa-store-alt"></i> Painel Vendedor</a>
      <% } else if (customer) { %>
        <a href="/conta"><i class="fas fa-user"></i> Minha Conta</a>
        <a href="/conta/sair"><i class="fas fa-sign-out-alt"></i> Sair</a>
      <% } else { %>
        <a href="/login"><i class="fas fa-user"></i> Entrar / Cadastrar</a>
        <a href="/seller/login"><i class="fas fa-store-alt"></i> Quero Vender</a>
      <% } %>
    </div>
  </div>
</nav>

<script>
document.addEventListener('DOMContentLoaded', function() {
  var hamburger = document.getElementById('hamburgerBtn');
  var mobileMenu = document.getElementById('mobileMenu');
  var overlay = document.getElementById('mobileOverlay');
  var closeBtn = document.getElementById('mobileClose');
  var searchToggle = document.getElementById('searchToggle');
  var searchContainer = document.getElementById('searchContainer');
  var catToggle = document.getElementById('catToggle');
  var catSubmenu = document.getElementById('catSubmenu');

  if (hamburger && mobileMenu && overlay) {
    function openMenu() {
      hamburger.classList.add('active');
      mobileMenu.classList.add('active');
      overlay.classList.add('active');
      document.body.style.overflow = 'hidden';
    }
    function closeMenu() {
      hamburger.classList.remove('active');
      mobileMenu.classList.remove('active');
      overlay.classList.remove('active');
      document.body.style.overflow = '';
    }
    hamburger.addEventListener('click', openMenu);
    closeBtn.addEventListener('click', closeMenu);
    overlay.addEventListener('click', closeMenu);
  }

  if (searchToggle && searchContainer) {
    searchToggle.addEventListener('click', function() {
      searchContainer.classList.toggle('active');
      if (searchContainer.classList.contains('active')) {
        var input = searchContainer.querySelector('input');
        if (input) input.focus();
      }
    });
  }

  if (catToggle && catSubmenu) {
    catToggle.addEventListener('click', function(e) {
      e.preventDefault();
      catSubmenu.classList.toggle('active');
      catToggle.querySelector('.fa-chevron-down').classList.toggle('rotated');
    });
  }
});

function toggleHackerTheme() {
  var active = document.body.classList.toggle('hacker-theme');
  localStorage.setItem('hacker-theme', active ? '1' : '0');
  var btn = document.getElementById('themeToggle');
  if (btn) btn.classList.toggle('active', active);
}
</script>

<script src="/js/notifications.js"></script>

<div class="top-ad" id="topAd">
  <div class="top-ad-content">
    <img class="top-ad-img" id="adImg" src="" alt="" style="display:none;">
    <span class="top-ad-label">PUBLICIDADE</span>
    <span class="top-ad-text" id="adText"></span>
  </div>
  <button class="top-ad-close" id="adClose" aria-label="Fechar">&times;</button>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
  var adText = document.getElementById('adText');
  var adClose = document.getElementById('adClose');
  var topAd = document.getElementById('topAd');
  var adImg = document.getElementById('adImg');
  var mc = document.querySelector('.main-content');
  var adData = null;
  var adStartTime = 0;

  function showAd(ad) {
    if (!ad || !adText || !topAd) return;
    adData = ad;
    adStartTime = Date.now();
    adText.innerHTML = ad.text || ad.title || '';
    if (adImg) {
      if (ad.image) { adImg.src = ad.image; adImg.style.display = ''; topAd.classList.add('has-img'); }
      else { adImg.src = ''; adImg.style.display = 'none'; topAd.classList.remove('has-img'); }
    }
    topAd.style.display = '';
    if (mc) mc.style.paddingTop = (topAd.offsetHeight || 32) + 'px';
    adClose.disabled = true;
    adClose.style.opacity = '0.3';
    adClose.style.cursor = 'default';

    if (ad.link) {
      topAd.querySelector('.top-ad-content').style.cursor = 'pointer';
      topAd.querySelector('.top-ad-content').onclick = function(e) {
        if (e.target.tagName !== 'A' && ad.link && ad.link.match(/^https?:\/\//i)) {
          window.open(ad.link, '_blank');
        }
      };
    }

    var duration = (ad.display_duration || 15) * 1000;
    setTimeout(function() {
      adClose.disabled = false;
      adClose.style.opacity = '1';
      adClose.style.cursor = 'pointer';
    }, duration);
  }

  function hideAd(permanent) {
    if (topAd) topAd.style.display = 'none';
    if (mc) mc.style.paddingTop = '0';
    if (permanent && adData) {
      var expires = Date.now() + ((adData.cooldown || 86400) * 1000);
      localStorage.setItem('adClosed', expires.toString());
      localStorage.setItem('adId', adData.id.toString());
    }
  }

  var closedTime = localStorage.getItem('adClosed');
  var closedAdId = localStorage.getItem('adId');

  if (closedTime && parseInt(closedTime) > Date.now()) {
    if (topAd) topAd.style.display = 'none';
  } else {
    localStorage.removeItem('adClosed');
    localStorage.removeItem('adId');
    fetch('/api/ads').then(function(r) { return r.json(); }).then(function(ad) {
      if (ad && ad.id && closedAdId && parseInt(ad.id) === parseInt(closedAdId) && closedTime && parseInt(closedTime) > Date.now()) {
        if (topAd) topAd.style.display = 'none';
        return;
      }
      showAd(ad);
    }).catch(function() {
      if (topAd) topAd.style.display = 'none';
    });
  }

  if (adClose && topAd) {
    adClose.addEventListener('click', function() {
      if (adClose.disabled) return;
      hideAd(true);
    });
  }
});
</script>

<main class="main-content">
<script src="/js/attraction.js"></script>
